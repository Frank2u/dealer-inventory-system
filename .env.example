# Server Configurations
PORT=5000
NODE_ENV=development

# JWT Secrets (for authentication)
JWT_SECRET=super-secret-key-12345

# Database URL
# For Local Development (SQLite - Zero Configuration Fallback):
DATABASE_URL="file:./dev.db"

# For Production (PostgreSQL):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/dealer_inventory?schema=public"
