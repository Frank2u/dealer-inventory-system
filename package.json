{
  "name": "dealer-inventory-system",
  "version": "1.0.0",
  "description": "Dealer Inventory, Delivery, Billing and Payment Tracking System",
  "type": "module",
  "scripts": {
    "setup": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build:client": "npm run build --prefix client",
    "start:server": "npm start --prefix server",
    "db:migrate": "cd server && prisma migrate dev",
    "db:seed": "cd server && prisma db seed",
    "db:studio": "cd server && prisma studio",
    "use-sqlite": "node scripts/switch-db.js sqlite",
    "use-postgres": "node scripts/switch-db.js postgres"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
